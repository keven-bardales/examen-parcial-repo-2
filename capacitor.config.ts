import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'examen-parcial-repo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
